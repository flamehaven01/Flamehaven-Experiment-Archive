mica_spec: "0.2.6"
name: qsot-harness-verification
mode: memory_injection
description: "MICA Package for QSOT-Harness v2.1.0 (honest reconstruction; supersedes toe-test-0057)."

layers:
  - name: archive
    path: memory/qsot-harness.mica.archive.json
    format: json
    loading_hint: always
  - name: playbook
    path: memory/qsot-harness-playbook.md
    format: markdown
    loading_hint: always

invocation_protocol:
  primary_pattern: readme_protocol

update_triggers:
  - on_explicit_save
